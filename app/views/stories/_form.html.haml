%div.small_container
  = simple_form_for [@project, @story_type, @story], :html => {:class => "big_form"} do |f|
    -if @story.errors.any?
      .alert-message.block-message.error
        %p= "#{pluralize(@story.errors.count, "error")} prohibited this story_type from being saved:"
        %ul
          - @story.errors.full_messages.each do |msg|
            %li= msg

    %div= f.input_field :name
    %br
    %div= f.button :submit, :class => "btn primary"
